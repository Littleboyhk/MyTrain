<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Location is requested at runtime, only when the user opts into
         "Inside this train?" or offline tracking — never on launch. COARSE
         covers cell-tower mode; FINE is requested only for the opt-in GPS mode. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- OFFLINE TRACKING: continuing to follow the train while the screen is off
         or the app is backgrounded.

         Android 10 (API 29) onwards, location updates outside the foreground
         require BOTH a background-location grant AND a foreground service, and
         from Android 14 (API 34) that service must declare a type. geolocator's
         own manifest contributes the <service> element with
         foregroundServiceType="location"; the permissions belong to the app, so
         they are declared here.

         WAKE_LOCK backs `ForegroundNotificationConfig.enableWakeLock`, without
         which the OS suspends the sampling loop between screen-on periods and
         the position silently stops advancing.

         PLAY STORE NOTE: ACCESS_BACKGROUND_LOCATION requires a prominent-
         disclosure declaration and review before release. The app degrades
         gracefully without it — tracking simply pauses when backgrounded (the
         same behaviour as iOS "While Using the App"), so it is not load-bearing
         for the foreground experience. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:label="My Train"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
